<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Set</title>
    <link rel="stylesheet" href="/css/main.css">
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f3f4f6;
            color: #1f2937;
        }

        .flex-container {
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
        }

        .form-container {
            width: 100%;
            max-width: 24rem;
            padding: 2rem;
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        h2 {
            font-size: 1.5rem;
            font-weight: bold;
            margin-bottom: 1rem;
            color: #4c51bf;
        }

        label {
            display: block;
            font-size: 0.875rem;
            margin-bottom: 0.5rem;
            color: #4a5568;
        }

        input, select {
            width: 100%;
            padding: 0.75rem;
            margin-bottom: 1rem;
            border: 1px solid #d1d5db;
            border-radius: 4px;
            font-size: 1rem;
        }

        button {
            background-color: #4c51bf;
            color: #fff;
            padding: 1rem;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 1rem;
        }

        button:hover {
            background-color: #4338ca;
        }

        .btn-danger {
            background-color: #e53e3e;
        }

        .btn-danger:hover {
            background-color: #c53030;
        }
    </style>
</head>
<body>
    <%- include('partials/navbar', { page: '/lego/editSet' }) %>
    <div class="flex-container">
        <div class="form-container">
            <h2>Edit Set</h2>
            <form action="/lego/editSet" method="POST">
                <div class="mb-4">
                    <label for="set_num" class="block text-sm font-medium text-gray-600">Set Number:</label>
                    <input type="text" name="set_num" value="<%= set.set_num %>" readonly class="mt-1 p-2 border rounded-md focus:outline-none focus:ring focus:border-blue-300 cursor-not-allowed">
                </div>

                <div class="mb-4">
                    <label for="name" class="block text-sm font-medium text-gray-600">Name:</label>
                    <input type="text" name="name" value="<%= set.name %>" required class="mt-1 p-2 border rounded-md focus:outline-none focus:ring focus:border-blue-300">
                </div>

                <div class="mb-4">
                    <label for="year" class="block text-sm font-medium text-gray-600">Year:</label>
                    <input type="number" name="year" value="<%= set.year %>" required class="mt-1 p-2 border rounded-md focus:outline-none focus:ring focus:border-blue-300">
                </div>

                <div class="mb-4">
                    <label for="num_parts" class="block text-sm font-medium text-gray-600">Number of Parts:</label>
                    <input type="number" name="num_parts" value="<%= set.num_parts %>" required class="mt-1 p-2 border rounded-md focus:outline-none focus:ring focus:border-blue-300">
                </div>

                <div class="mb-4">
                    <label for="img_url" class="block text-sm font-medium text-gray-600">Image URL:</label>
                    <input type="url" name="img_url" value="<%= set.img_url %>" required class="mt-1 p-2 border rounded-md focus:outline-none focus:ring focus:border-blue-300">
                </div>

                <div class="mb-4">
                    <label for="theme_id" class="block text-sm font-medium text-gray-600">Theme:</label>
                    <select name="theme_id" required class="mt-1 p-2 border rounded-md focus:outline-none focus:ring focus:border-blue-300">
                        <% themes.forEach(theme => { %>
                            <option value="<%= theme.id %>" <%= (set.theme_id == theme.id) ? "selected" : "" %>>
                                <%= theme.name %>
                            </option>
                        <% }) %>
                    </select>
                </div>
            
                <button type="submit" class="btn btn-primary">Update Set</button>
                <a href="/lego/deleteSet/<%= set.set_num %>" class="btn btn-danger">Delete Set</a>
            </form>
        </div>
    </div>
</body>
</html>
