<<<<<<< HEAD
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lego Collection</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
    <%- include('partials/navbar', { page: '/lego/addSet' }) %>

    <div class="container mx-auto p-4">
        <h1 class="text-4xl font-bold text-gray-800 mb-8 text-center">Add Set</h1>

        <form action="/lego/addSet" method="POST" class="max-w-md mx-auto bg-white p-8 rounded shadow-md">
            <!-- Name input -->
            <div class="mb-4">
                <label for="name" class="block text-gray-700 text-sm font-semibold mb-2">Name:</label>
                <input type="text" id="name" name="name" class="border-2 border-gray-300 p-3 w-full focus:outline-none focus:border-blue-500" required>
            </div>

            <!-- Year input -->
            <div class="mb-4">
                <label for="year" class="block text-gray-700 text-sm font-semibold mb-2">Year:</label>
                <input type="number" id="year" name="year" class="border-2 border-gray-300 p-3 w-full focus:outline-none focus:border-blue-500" required>
            </div>

            <!-- Num Parts input -->
            <div class="mb-4">
                <label for="num_parts" class="block text-gray-700 text-sm font-semibold mb-2">Number of Parts:</label>
                <input type="number" id="num_parts" name="num_parts" class="border-2 border-gray-300 p-3 w-full focus:outline-none focus:border-blue-500" required>
            </div>

            <!-- Img URL input -->
            <div class="mb-4">
                <label for="img_url" class="block text-gray-700 text-sm font-semibold mb-2">Image URL:</label>
                <input type="url" id="img_url" name="img_url" class="border-2 border-gray-300 p-3 w-full focus:outline-none focus:border-blue-500" required>
            </div>

            <!-- Theme select -->
            <div class="mb-4">
                <label for="theme_id" class="block text-gray-700 text-sm font-semibold mb-2">Theme:</label>
                <select id="theme_id" name="theme_id" class="border-2 border-gray-300 p-3 w-full focus:outline-none focus:border-blue-500" required>
                    <% themes.forEach(theme => { %>
                        <option value="<%= theme.id %>"><%= theme.name %></option>
                    <% }) %>
                </select>
            </div>

            <!-- Set Num input -->
            <div class="mb-8">
                <label for="set_num" class="block text-gray-700 text-sm font-semibold mb-2">Set Number:</label>
                <input type="text" id="set_num" name="set_num" class="border-2 border-gray-300 p-3 w-full focus:outline-none focus:border-blue-500" required>
            </div>

            <!-- Submit button -->
            <button type="submit" class="bg-blue-500 text-white px-6 py-3 rounded-full hover:bg-blue-700 focus:outline-none">Submit</button>
        </form>
    </div>
</body>
</html>
=======
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lego Collection</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
    <%- include('partials/navbar', { page: '/lego/addSet' }) %>

    <div class="container mx-auto p-4">
        <h1 class="text-4xl font-bold text-gray-800 mb-8 text-center">Add Set</h1>

        <form action="/lego/addSet" method="POST" class="max-w-md mx-auto bg-white p-8 rounded shadow-md">
            <!-- Name input -->
            <div class="mb-4">
                <label for="name" class="block text-gray-700 text-sm font-semibold mb-2">Name:</label>
                <input type="text" id="name" name="name" class="border-2 border-gray-300 p-3 w-full focus:outline-none focus:border-blue-500" required>
            </div>

            <!-- Year input -->
            <div class="mb-4">
                <label for="year" class="block text-gray-700 text-sm font-semibold mb-2">Year:</label>
                <input type="number" id="year" name="year" class="border-2 border-gray-300 p-3 w-full focus:outline-none focus:border-blue-500" required>
            </div>

            <!-- Num Parts input -->
            <div class="mb-4">
                <label for="num_parts" class="block text-gray-700 text-sm font-semibold mb-2">Number of Parts:</label>
                <input type="number" id="num_parts" name="num_parts" class="border-2 border-gray-300 p-3 w-full focus:outline-none focus:border-blue-500" required>
            </div>

            <!-- Img URL input -->
            <div class="mb-4">
                <label for="img_url" class="block text-gray-700 text-sm font-semibold mb-2">Image URL:</label>
                <input type="url" id="img_url" name="img_url" class="border-2 border-gray-300 p-3 w-full focus:outline-none focus:border-blue-500" required>
            </div>

            <!-- Theme select -->
            <div class="mb-4">
                <label for="theme_id" class="block text-gray-700 text-sm font-semibold mb-2">Theme:</label>
                <select id="theme_id" name="theme_id" class="border-2 border-gray-300 p-3 w-full focus:outline-none focus:border-blue-500" required>
                    <% themes.forEach(theme => { %>
                        <option value="<%= theme.id %>"><%= theme.name %></option>
                    <% }) %>
                </select>
            </div>

            <!-- Set Num input -->
            <div class="mb-8">
                <label for="set_num" class="block text-gray-700 text-sm font-semibold mb-2">Set Number:</label>
                <input type="text" id="set_num" name="set_num" class="border-2 border-gray-300 p-3 w-full focus:outline-none focus:border-blue-500" required>
            </div>

            <!-- Submit button -->
            <button type="submit" class="bg-blue-500 text-white px-6 py-3 rounded-full hover:bg-blue-700 focus:outline-none">Submit</button>
        </form>
    </div>
</body>
</html>
>>>>>>> edbba4046de4222da18bf93e0d904b32a714f1cf
